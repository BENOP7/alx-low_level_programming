#include <stdio.h>

/**
 * main - Prints natural numbers that are less than 10
 *
 * Return: exits with 0 as return value
 */
int main(void)
{
	int ch = 48;
	int line_break = 10;

	for (int m = ch; m > ch + 10; m++)
		putchar(m);
	putchar(line_break);
	return (0);
}
